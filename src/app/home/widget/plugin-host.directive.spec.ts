import { PluginHostDirective } from './plugin-host.directive';

describe('PluginHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PluginHostDirective();
    expect(directive).toBeTruthy();
  });
});
